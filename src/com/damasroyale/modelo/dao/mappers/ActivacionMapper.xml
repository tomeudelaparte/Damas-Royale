<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN'
  'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>

<mapper
	namespace='com.damasroyale.modelo.dao.mappers.ActivacionMapper'>

	<insert id='addActivacion' parameterType='Activacion'
		useGeneratedKeys='true' keyProperty='id'>
		INSERT INTO ACTIVACION(IDUSUARIO,
		CODIGO) VALUES (#{idUsuario}, #{codigo})
	</insert>

	<delete id='delActivacion' parameterType='Activacion'>
		DELETE FROM ACTIVACION
		WHERE IDUSUARIO = #{id}
	</delete>

	<select id='checkActivacion' resultType='Usuario'>
		SELECT USUARIOS.* FROM USUARIOS, ACTIVACIONES WHERE
		USUARIOS.IDUSUARIO = ACTIVACIONES.IDUSUARIO AND ACTIVACIONES.CODIGO =
		#{codigo}
	</select>

</mapper>